def min(a, b):
    if a > b:   #  okaaay so if a is bigger than bb
        return a  # then a eats b so a is the minimum... right?
    else:
        return b # and here is the opposite


def max(a, b):  # same thing but reversed.. lelelelele
    if a > b:
        return b
    # i skip the else, i am so smart
    return a
